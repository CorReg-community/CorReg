#function that computes R-squared and adjusted R-squared for a givent dataset and linear regression formula

Rsquare<-function(variables,coef,data){
  data=data[,names(data)%in%variables]
  
  
  return(0)
}


